{
  "name": "mercury",
  "version": "0.2.4",
  "description": "Mercury Webhook",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --ip 127.0.0.1 --port 3000",
    "remote": "wrangler dev --remote --ip 127.0.0.1 --port 3000",
    "deploy": "wrangler deploy",
    "login": "wrangler login",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.x.x",
    "typescript": "5.0.x",
    "wrangler": "latest"
  },
  "dependencies": {
    "@aws-sdk/client-lightsail": "^3.328.0"
  }
}
