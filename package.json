{
  "name": "mercury",
  "version": "0.3.0",
  "description": "Mercury Webhook",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --ip 127.0.0.1 --port 3000",
    "remote": "wrangler dev --remote --ip 127.0.0.1 --port 3000",
    "deploy": "wrangler deploy",
    "login": "wrangler login",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.x.x",
    "typescript": "5.0.x",
    "wrangler": "3.6.x"
  },
  "dependencies": {
    "@aws-sdk/client-lightsail": "3.328.x"
  }
}
